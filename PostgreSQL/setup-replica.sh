#!/bin/bash
# This script is used to set up replica nodes - it's referenced in docker-compose-cluster.yml
# but the actual setup is handled by the container command

echo "Replica setup script - actual setup is handled by container initialization"
